<div class="get-start-area">
<%= form_with(model: testpage, local: true) do |form| %>
  <% if testpage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testpage.errors.count, "error") %> prohibited this testpage from being saved:</h2>

      <ul>
      <% testpage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="email">      
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :place %>
    <%= form.text_field :place %>
  </div>
</div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>
<div class="get-start-area">
<%= bootstrap_form_for(@testpage) do |f| %>
  <div class="email"> 
  <%= f.text_field :name %>
  </div>
  <%= f.text_field :place %>
  <%= f.submit "Log In" %>
<% end %>
</div>